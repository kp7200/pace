import 'package:flutter/material.dart';

class AppColors {
  // Core Identity
  static const Color canvasCream = Color(0xFFF3F0EE);
  static const Color canvasCream2 = Color(0xFFD5D2BD);
  static const Color inkBlack = Color(0xFF141413);
  
  // Specific Use Accents
  static const Color signalOrange = Color(0xFFCF4500); // Warning / Legal / Critical
  static const Color linkBlue = Color(0xFF3860BE); // Actionable links only
  
  // Neutral Tones
  static const Color transparent = Colors.transparent;
}
